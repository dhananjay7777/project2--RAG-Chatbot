# Lean Railway/API image deps (Hobby plan: ~4 GB image + low RAM).
# No torch / ST packages — deploy uses MF_RETRIEVAL_MODE=bm25.
# Local/dev and CI still use requirements.txt for full hybrid retrieval.
pydantic>=2.6,<3
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
requests>=2.32,<3
beautifulsoup4>=4.12,<5
lxml>=5.0,<6
rank-bm25>=0.2,<0.3
groq>=0.11,<1
fastapi>=0.110,<1
uvicorn>=0.29,<1
